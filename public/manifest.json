import './globals.css';

export const metadata = {
  title: 'LUNA',
  description: 'Private connection',
  manifest: '/manifest.json', // Подключаем паспорт
  appleWebApp: {
    capable: true,
    statusBarStyle: 'black-translucent',
    title: 'LUNA',
  },
  viewport: {
    width: 'device-width',
    initialScale: 1,
    maximumScale: 1,
    userScalable: false, // Запрещаем зум пальцами (важно для ощущения приложения)
  },
  themeColor: '#000000',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        {/* Исправление для старых айфонов */}
        <meta name="mobile-web-app-capable" content="yes" />
      </head>
      <body className="bg-black text-white antialiased">
        {children}
      </body>
    </html>
  );
}
